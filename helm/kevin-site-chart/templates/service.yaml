apiVersion: v1
kind: Service
metadata:
  name: {{ include "kevin-site-chart.fullname" . }}
  labels:
    {{- include "kevin-site-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kevin-site-chart.selectorLabels" . | nindent 4 }}
